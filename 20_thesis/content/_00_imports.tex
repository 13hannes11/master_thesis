\usepackage[british]{babel} % manages culturally-determined typographical (and other) rules for a wide range of languages.


\usepackage[T1]{fontenc}
\usepackage[full]{textcomp} %  provide many text symbols (such as baht, bullet, copyright, musicalnote, onequarter, section, and yen), in the TS1 encoding
\usepackage[utf8]{inputenc}
\usepackage{microtype} % for improved space handling (might be used to fix bibliography)

\usepackage[babel]{csquotes}

\usepackage{float}

\usepackage[
    dashed=false,
    bibstyle=ieee,
    citestyle=numeric-comp,
    sorting=none,
]{biblatex}
\usepackage{placeins} % for FloatBarrier
\usepackage[hidelinks]{hyperref} % for using autoref

\usepackage{amsmath}
\usepackage{amssymb}

\usepackage{todonotes}

\usepackage{acro} % acronyms
\acsetup{
    make-links=true
}

\usepackage{caption}
\usepackage{subcaption}

\usepackage{tablefootnote}

\usepackage[mediumspace,mediumqspace,squaren]{SIunits}

\usepackage{dirtytalk} % quotes using \say{}

\usepackage{tikz}
\usetikzlibrary{calc}

\usepackage{listings}
\lstset{language=Python,
       frame=single,
       basicstyle=\footnotesize\ttfamily,
}

\usepackage{etoolbox} % for \ifnumcomp
\usepackage{listofitems} % for \readlist to create arrays

\addbibresource{references.bib}

% make sure autoref only uses sections not subsections
\addto\extrasbritish{
    \let\subsectionautorefname\sectionautorefname 	\let\subsubsectionautorefname\sectionautorefname
}

% MATH helper commands
\newcommand*{\mat}[1]{\mathbf{#1}}
\newcommand{\func}[1]{\operatorname{\mathit{#1}}}  % or \mathrm
\newcommand{\const}[1]{\mathit{#1}}                % or \mathrm
\newcommand{\wsub}[1]{\mathrm{#1}}                 % 'word' subscript
\newcommand{\wfunc}[1]{\mathrm{#1}}  
\newcommand{\wvar}[1]{\mathrm{#1}}  

% Acknowledgements
\makeatletter
\newcommand\ackname{Acknowledgements}
\if@titlepage
  \newenvironment{acknowledgements}{%
      \titlepage
      \null\vfil
      \@beginparpenalty\@lowpenalty
      \begin{center}%
        \bfseries \ackname
        \@endparpenalty\@M
      \end{center}}%
     {\par\vfil\null\endtitlepage}
\else
  \newenvironment{acknowledgements}{%
      \if@twocolumn
        \section*{\abstractname}%
      \else
        \small
        \begin{center}%
          {\bfseries \ackname\vspace{-.5em}\vspace{\z@}}%
        \end{center}%
        \quotation
      \fi}
      {\if@twocolumn\else\endquotation\fi}
\fi
\makeatother


\newcommand\emptypage{
  \FloatBarrier
  \newpage 
  \thispagestyle{empty}
  \addtocounter{page}{1}
  \ % The empty page
  \newpage
}

